#!/bin/sh
# Git bu dosyayi calistirir. Commit mesaji kontrolu PowerShell scriptindedir.
REPO_ROOT=$(git rev-parse --show-toplevel)
powershell.exe -ExecutionPolicy Bypass -File "$REPO_ROOT/scripts/commit-mesaji-kontrol.ps1" "$1"
