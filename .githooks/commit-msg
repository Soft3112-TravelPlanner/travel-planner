#!/bin/sh
# Git bu dosyayi calistirir. Commit mesaji kontrolu PowerShell scriptindedir.
HOOK_DIR="$(cd "$(dirname "$0")" && pwd)"
powershell.exe -ExecutionPolicy Bypass -File "$HOOK_DIR/../scripts/commit-mesaji-kontrol.ps1" "$1"
